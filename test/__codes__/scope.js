class Scope extends Component {

  getFooterButs() {
    return (
      <div>
        <button onClick={() => {}}/>
        <button onClick={() => {}}/>
      </div>
    )
  }

  render() {
    return (
      <div>
        <button onClick={() => {}}/>
      </div>
    )
  }
}
